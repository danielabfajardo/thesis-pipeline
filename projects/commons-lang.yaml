project_id: "commons-lang"
project_display_name: "Apache Commons Lang"

# SonarQube creationDate = when the analysis was run, not when the code was written.
# This instance was analyzed in April 2026, so both SonarQube bounds are null
# (fetch all open issues regardless of analysis date).
# GitHub churn and SATD are scoped to the 2023–2024 commit window via start/end dates
# — those extractors use git history dates, not SonarQube analysis dates.
time_period:
  start_date: null
  end_date: null

sonarqube:
  host: "http://localhost:9000"
  project_key: "commons-lang"
  token: "squ_53121277353daf94c6ddc80d0e33ff5ff5de26d1"

github:
  repo_owner: "apache"
  repo_name: "commons-lang"
  branch: "master"

components:
  run_sonarqube: true
  run_github_churn: true
  run_satd: true
  run_llm_ranking: true
