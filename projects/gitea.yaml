project_id: "gitea"
project_display_name: "Gitea"

# Scanned from go-gitea/gitea v1.22.6 (released Nov 2024).
# SonarQube analysis run May 2026 — creationDate reflects analysis date, not code date.
# Both bounds null: fetch all open issues.
# 3,648 total issues (BLOCKER:9, CRITICAL:2517, MAJOR:445, MINOR:459, INFO:218).
# No severity filter needed — total is well under SonarQube's 10k pagination cap.
time_period:
  start_date: null
  end_date: null

sonarqube:
  host: "http://localhost:9000"
  project_key: "gitea"
  token: "squ_53121277353daf94c6ddc80d0e33ff5ff5de26d1"

github:
  repo_owner: "go-gitea"
  repo_name: "gitea"
  branch: "main"

components:
  run_sonarqube: true
  run_github_churn: true
  run_satd: true
  run_llm_ranking: true
